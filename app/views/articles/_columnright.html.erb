<h3>Entradas Recientes</h3>
<div id="articulos_recientes">
  <ul>
    <% @lastArticles.each do |art| %>
      <li><span class="glyphicon glyphicon-menu-right" aria-hidden="true"> <%= link_to art.title, art %></li> 
    <% end %>
  </ul>
</div>

<h3>Nuestras Imágenes</h3>
<div id="ultimas_imagenes">
  <% @imagenes.each do |img| %>
    <%= link_to(image_tag(img.newimg(:thumb)), img) %>
  <% end %>
	
</div>

<h3>Últimos Comentarios</h3>
<div id="ultimos_comentarios">

	<ul>
	<% @comments.each do |cmt| %>
		<li>- "<%= cmt.comment_short %> ..." por <%= cmt.nick %></li>
	<% end %>
	</ul>
</div>